@model eProjectSemester3_1.Areas.Shop.ViewModels.ShopEstateEditViewModel
@{
    ViewBag.Title = "Edit Estate";
}
@Html.Partial("_EstateNavButtons")
<div class="panel">
    <div class="panel-heading">
        <h5>@ViewBag.Title</h5>
    </div>
    <div class="panel-content">
        @using (Html.BeginForm("Edit", "ShopEstate", FormMethod.Post, new { enctype = "multipart/form-data" }))
        {
            @Html.ValidationSummary(false)
            @Html.AntiForgeryToken()
            @Html.HiddenFor(x => x.Id);

            @Html.Partial("_EstateFields", Model)

            <div class="submit-holder">
                <br />
                <input type="submit" class="btn-mvc-green btn-mvc-large" value="Update" />
            </div>
        }
    </div>
</div>